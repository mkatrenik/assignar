export enum ImgurImageSource {
  subreddit = 'subreddit',
  album = 'album'
}

export type TDataUrl = string
export type TImageSize = { width: number; height: number }

export enum TileRenderer {
  circleRenderer = 'circleRenderer',
  halfToneRenderer = 'halfToneRenderer'
}
